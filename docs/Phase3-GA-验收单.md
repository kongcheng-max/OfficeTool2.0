# OfficeTool 第三阶段（瑶光）GA 验收单

> **验收版本**: v2.0.0-rc | **验收日期**: 2026-07-__ | **验收人**: QA 测试部 ____
> **关联**: [Phase 3 规划](./05-第三阶段-生产就绪详细规划.md) | [Phase 2 验收单](./Phase2-手动验收单.md) ✅
>
> **使用说明**: 逐项手动测试，通过打 ✅，不通过打 ❌ 并备注问题。🔴 = 阻塞 GA 发布项。

---

## 一、性能优化 — 大文件 & 流式解析（Week 9）

### 1.1 PDF 大文件流式解析

| # | 测试项 | 操作步骤 | 预期结果 | 结果 | 备注 |
|---|--------|---------|---------|:--:|------|
| 1 | **大 PDF 解析不崩** 🔴 | 上传一个 50MB+ 的 PDF（可用 `《Java并发...》.pdf` 387KB 或更大文件） | 解析成功完成，状态变为 ready，不报 OOM | | |
| 2 | **PDF 分页批处理** | 查看解析日志 | 日志显示 "PDF 流式解析: xxx (pages x-y / total, batch=20)" | | |
| 3 | **页面范围参数** | 通过 API 指定 start_page + max_pages | 仅解析指定页面范围 | | |

### 1.2 文档布局分析

| # | 测试项 | 操作步骤 | 预期结果 | 结果 | 备注 |
|---|--------|---------|---------|:--:|------|
| 4 | **多栏布局识别** | 上传双栏排版的 PDF | 文字按正确阅读顺序提取，不混排 | | |
| 5 | **页眉页脚过滤** | 上传含页眉页脚的文档 → 查看解析后的 chunk 内容 | 页眉页脚文字不出现或标记为 header/footer | | |

### 1.3 解析缓存

| # | 测试项 | 操作步骤 | 预期结果 | 结果 | 备注 |
|---|--------|---------|---------|:--:|------|
| 6 | **重复文件跳过** 🔴 | 上传文件 A → 解析完成 → 再次上传同一个文件 A | 第二次跳过解析，直接复用缓存，日志显示命中缓存 | | |
| 7 | **MD5 去重** | 上传内容相同但文件名不同的两个文件 | 第二个文件被识别为重复，返回提示 | | |

### 1.4 Cross-encoder 精排

| # | 测试项 | 操作步骤 | 预期结果 | 结果 | 备注 |
|---|--------|---------|---------|:--:|------|
| 8 | **精排生效** 🔴 | 在知识库中提问 "微服务的优缺点" → 检查日志 | 日志显示 "Cross-encoder 精排完成"，结果排序与纯 RRF 不同 | | |
| 9 | **精排降级** | 环境无 FlagEmbedding 时提问 | 日志显示降级为 bi-encoder，问答功能不受影响 | | |

### 1.5 查询改写

| # | 测试项 | 操作步骤 | 预期结果 | 结果 | 备注 |
|---|--------|---------|---------|:--:|------|
| 10 | **改写扩展召回** | 提问后用简短关键词对比检索结果 | 查询改写生成变体后召回量 ≥ 原始查询 | | |
| 11 | **改写日志** | 查看检索日志 | 日志显示 "variants=N"，N ≥ 1 | | |

---

## 二、系统级优化（Week 10）

### 2.1 LLM 问答缓存

| # | 测试项 | 操作步骤 | 预期结果 | 结果 | 备注 |
|---|--------|---------|---------|:--:|------|
| 12 | **相同问题缓存命中** 🔴 | 提问问题 A → 等待回答 → 立即再次提问完全相同的问题 A | 第二次响应明显更快（命中缓存），答案内容一致 | | |
| 13 | **不同问题不命中** | 提问问题 A → 提问问题 B（不同措辞） | 问题 B 不命中缓存，正常调用 LLM | | |

### 2.2 Nginx 生产配置

| # | 测试项 | 操作步骤 | 预期结果 | 结果 | 备注 |
|---|--------|---------|---------|:--:|------|
| 14 | **gzip 压缩** | 浏览器 DevTools → Network → 查看 /api/ 响应头 | `Content-Encoding: gzip`，传输大小明显小于原始大小 | | |
| 15 | **静态资源缓存** | 浏览器 DevTools → Network → 查看 /assets/ 下资源的响应头 | `Cache-Control: public, immutable`，状态码 200 (from disk cache) 或 304 | | |

### 2.3 数据库索引优化

| # | 测试项 | 操作步骤 | 预期结果 | 结果 | 备注 |
|---|--------|---------|---------|:--:|------|
| 16 | **文档列表查询性能** | 知识库中有 50+ 文档时，请求 GET /documents | 响应时间无明显增长（< 200ms） | | |
| 17 | **MD5 去重查询** | 上传重复文件 | 去重检测不拖慢上传速度 | | |

### 2.4 文档状态细化

| # | 测试项 | 操作步骤 | 预期结果 | 结果 | 备注 |
|---|--------|---------|---------|:--:|------|
| 18 | **状态流转** 🔴 | 上传文档 → 观察状态变化 | 状态流转：uploaded → processing → parsed → ready（Embedding 完成后） | | |
| 19 | **前端状态显示** | 文档列表页面观察状态标签 | 各个状态在前端正确显示对应标签和颜色 | | |

---

## 三、安全加固（Week 11）

### 3.1 RBAC 角色权限

| # | 测试项 | 操作步骤 | 预期结果 | 结果 | 备注 |
|---|--------|---------|---------|:--:|------|
| 20 | **admin 全部权限** 🔴 | 用 admin 账号登录 → 依次测试：创建KB/上传文档/管理用户/查看审计日志 | 所有操作成功 | | |
| 21 | **editor 文档管理** 🔴 | 用 editor 账号登录 → 上传/删除文档 → 尝试访问管理后台 | 文档操作成功；访问管理后台返回 403 | | |
| 22 | **viewer 只读** 🔴 | 用 viewer 账号登录 → 尝试上传文档/删除KB | 返回 403 Forbidden "权限不足"；问答功能正常 | | |
| 23 | **未登录拦截** | 退出登录后直接访问 API | 返回 401 Unauthorized | | |

### 3.2 操作审计日志

| # | 测试项 | 操作步骤 | 预期结果 | 结果 | 备注 |
|---|--------|---------|---------|:--:|------|
| 24 | **操作自动记录** 🔴 | 执行若干操作（上传文档/删除文档/创建KB/提问） | 数据库中 `audit_logs` 表有对应记录，含 user/action/target/IP/time | | |
| 25 | **审计日志查询** | admin 登录 → GET /api/v1/admin/audit-logs | 返回分页的审计日志列表，可筛选 | | |
| 26 | **非 admin 不可查** | editor 或 viewer 请求 /api/v1/admin/audit-logs | 返回 403 | | |

### 3.3 API 限流

| # | 测试项 | 操作步骤 | 预期结果 | 结果 | 备注 |
|---|--------|---------|---------|:--:|------|
| 27 | **超限返回 429** 🔴 | 短时间内连续发送 100+ 次 API 请求 | 超过阈值后返回 `429 Too Many Requests` | | |
| 28 | **限流后恢复** | 收到 429 后等待 1 分钟 → 再次请求 | 恢复正常，请求成功 | | |

### 3.4 文档加密

| # | 测试项 | 操作步骤 | 预期结果 | 结果 | 备注 |
|---|--------|---------|---------|:--:|------|
| 29 | **加密存储** | 开启加密选项后上传文档 → 检查 MinIO/本地存储中的文件内容 | 存储的文件内容为密文，无法直接读取 | | |
| 30 | **解密读取** | 通过 API 下载/预览已加密文档 | 文档内容正确解密并返回 | | |

### 3.5 管理后台

| # | 测试项 | 操作步骤 | 预期结果 | 结果 | 备注 |
|---|--------|---------|---------|:--:|------|
| 31 | **管理后台入口** 🔴 | admin 登录 → 查看侧边栏/导航 | 显示"管理后台"入口 | | |
| 32 | **用户管理页面** | 进入管理后台 → 用户管理 | 显示用户列表，可查看用户角色 | | |
| 33 | **审计日志页面** | 进入管理后台 → 审计日志 | 显示操作日志列表，可按用户/操作类型筛选 | | |
| 34 | **非 admin 无入口** | editor 或 viewer 登录 | 侧边栏不显示管理后台入口 | | |

### 3.6 NL2Cypher 图谱自然语言问答

| # | 测试项 | 操作步骤 | 预期结果 | 结果 | 备注 |
|---|--------|---------|---------|:--:|------|
| 35 | **自然语言查图谱** | 在图谱页面输入自然语言问题（如"张建国与哪些公司有关联？"） | 生成 Cypher 查询并返回图谱结果 | | |
| 36 | **不支持的查询降级** | 输入与图谱无关的问题 | 返回错误提示，不崩溃 | | |

---

## 四、部署与工程化（Week 12）

### 4.1 生产环境部署

| # | 测试项 | 操作步骤 | 预期结果 | 结果 | 备注 |
|---|--------|---------|---------|:--:|------|
| 37 | **docker-compose.prod.yml** 🔴 | `docker compose -f docker-compose.prod.yml up -d` | 所有服务启动成功（PG+Redis+MinIO+ES+Neo4j+Milvus+App+Nginx+Celery） | | |
| 38 | **健康检查** | `curl http://localhost/api/health` | 返回 200，各组件健康 | | |
| 39 | **轻量部署仍可用** | 使用 deploy/deploy.sh 轻量模式部署 | 单机 SQLite 模式正常工作 | | |

### 4.2 URL Token 认证

| # | 测试项 | 操作步骤 | 预期结果 | 结果 | 备注 |
|---|--------|---------|---------|:--:|------|
| 40 | **文档下载认证** | 登录后点击文档下载 → 或在 URL 中加 `?token=xxx` | 下载成功，文件内容正确 | | |
| 41 | **预览 iframe 认证** | 文档预览页面嵌入 iframe 加载预览内容 | iframe 内容正常显示（通过 ?token 认证） | | |
| 42 | **无 token 拒绝** | 不带 token 直接访问下载/预览 URL | 返回 401 | | |

---

## 五、端到端回归测试

| # | 测试场景 | 操作步骤 | 预期结果 | 结果 | 备注 |
|---|---------|---------|---------|:--:|------|
| 43 | **全量服务启动** 🔴 | Docker Compose 启动全量 9 容器 → 等待就绪 | 所有容器 healthy | | |
| 44 | **完整用户旅程** 🔴 | 注册(admin) → 创建KB → 上传 PDF+DOCX+PPTX+图片 → 等待全链路 → 提问 → 查看图谱 → 查审计日志 | 全链路无报错，所有环节功能正常 | | |
| 45 | **权限矩阵验证** | 创建 admin/editor/viewer 三个用户 → 分别执行各类操作 | 权限边界清晰，403/401 返回正确 | | |
| 46 | **性能冒烟测试** | 知识库中 20+ 文档 → 提问 10 个问题 → 记录 P95 延迟 | 单次问答延迟 ≤ 10s（含 LLM 调用） | | |
| 47 | **限流保护验证** | 高频调用 API → 观察限流 → 等待恢复 | 429 出现且恢复后正常 | | |

---

## 六、文档与报告类交付物（非功能测试）

> 此类为文档/报告产出物，由对应部门提交归档即可，不涉及系统操作测试。

| # | 交付物 | 负责部门 | 提交状态 | 备注 |
|---|--------|:--:|:--:|------|
| D1 | 性能压测报告 | **测试部** | ⬜ | 50 QPS 并发，JMeter/Locust 脚本+报告 |
| D2 | 安全测试报告 | **测试部** | ⬜ | OWASP Top 10 自查 |
| D3 | 兼容性测试报告 | **测试部** | ⬜ | Chrome/Edge/Firefox/Safari 四浏览器 |
| D4 | CHANGELOG.md | **产品部** | ⬜ | v2.0 GA 版本变更记录 |
| D5 | Release Notes | **产品部** | ⬜ | v2.0 GA 发布说明 |
| D6 | 部署运维文档更新 | **产品部** | ⬜ | 更新 [10-部署与运维方案](./10-部署与运维方案.md) |
| D7 | 演示环境 / Demo 录制 | **产品部** | ⬜ | 功能演示 |

---

## 📊 验收统计

| 模块 | 总项 | 通过 | 不通过 | 通过率 |
|------|:--:|:--:|:--:|:--:|
| 一、性能优化（流式+缓存+精排+改写） | 11 | | | |
| 二、系统级优化（缓存+Nginx+索引+状态） | 8 | | | |
| 三、安全加固（RBAC+审计+限流+加密+管理后台+NL2Cypher） | 17 | | | |
| 四、部署与工程化（生产部署+URL认证） | 6 | | | |
| 五、端到端回归 | 5 | | | |
| **功能测试合计** | **47** | | | |
| 六、文档与报告交付物 | 7 | | | |
| **总计** | **54** | | | |

---

## 验收结论

- [ ] ✅ **通过** — 所有 🔴 阻塞项（15 项）全部通过，文档报告类交付物全部提交
- [ ] ⚠️ **有条件通过** — 功能测试全部通过，但文档/报告类交付物有 ≤ 3 项未完成
- [ ] ❌ **不通过** — 存在 🔴 阻塞项不通过，需修复后重新验收

**🔴 阻塞项清单（15 项）**:

> #1 大PDF解析 | #6 重复文件跳过 | #8 Cross-encoder精排 | #12 相同问题缓存 | #18 状态流转
> #20 admin权限 | #21 editor权限 | #22 viewer权限 | #24 操作审计 | #27 API限流
> #31 管理后台入口 | #37 生产部署 | #40 文档下载认证 | #43 全量启动 | #44 完整用户旅程

---

**验收人签字**: __________ &nbsp;&nbsp;&nbsp; **日期**: 2026-07-__

> 📝 验收完成后请将签字版归档至 `docs/` 目录。
